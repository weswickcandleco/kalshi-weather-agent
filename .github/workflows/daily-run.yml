name: Evening Auto-Trade (Lows)

on:
  workflow_dispatch: # Triggered by Cloudflare Worker cron at 8:12 PM CT

permissions:
  contents: write

concurrency:
  group: kalshi-bot
  cancel-in-progress: false

jobs:
  auto-trade:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Write secrets to files
        env:
          KALSHI_PROD_PEM: ${{ secrets.KALSHI_PROD_PRIVATE_KEY }}
          KALSHI_DEMO_PEM: ${{ secrets.KALSHI_DEMO_PRIVATE_KEY }}
        run: |
          echo "$KALSHI_PROD_PEM" > kalshi-prod-private-key.pem
          echo "$KALSHI_DEMO_PEM" > kalshi-private-key.pem
          chmod 600 *.pem

      - name: Create .env
        env:
          KALSHI_PROD_API_KEY_ID: ${{ secrets.KALSHI_PROD_API_KEY_ID }}
          KALSHI_DEMO_API_KEY_ID: ${{ secrets.KALSHI_DEMO_API_KEY_ID }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          cat > .env <<EOF
          KALSHI_PROD_API_KEY_ID=${KALSHI_PROD_API_KEY_ID}
          KALSHI_PROD_PRIVATE_KEY_PATH=./kalshi-prod-private-key.pem
          KALSHI_DEMO_API_KEY_ID=${KALSHI_DEMO_API_KEY_ID}
          KALSHI_DEMO_PRIVATE_KEY_PATH=./kalshi-private-key.pem
          DISCORD_WEBHOOK_URL=${DISCORD_WEBHOOK_URL}
          EOF

      - name: Run auto-trader (lows only)
        run: python auto_trade.py --live --market-type low

      - name: Commit updated trades.db and dashboard data
        run: |
          git config user.name "kalshi-bot"
          git config user.email "bot@kalshi-agent.local"
          git pull --rebase origin main || true
          git add trades.db dashboard/data.json
          git diff --cached --quiet || git commit -m "bot: trades.db + dashboard after daily run $(date -u +%Y-%m-%d)"
          git push
